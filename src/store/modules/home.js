import { createSlice } from "@reduxjs/toolkit";

const homeSlice = createSlice({
  name: "home",
  initialState: {
    message: 'hello world'
  },
  reducers: {

  }
})

export default homeSlice.reducer
